# StopShorts - 新機能仕様書 v2.0

## 概要

本文書では、v2.0で追加する以下の機能について詳細な仕様を定義する：

1. **インパクト可視化機能** - 人生で失う時間の計算・表示
2. **カウンセリング・コーチング機能** - 依存克服のガイダンス
3. **目標設定・日誌機能** - 代替活動の設定とチェックイン
4. **インタラクティブチュートリアル** - アプリUIを再現した使用方法説明
5. **3日間フリートライアル** - 課金プランのデフォルトオプション

---

## 1. インパクト可視化機能

### 1.1 概要

オンボーディング時に現在の使用状況をヒアリングし、「このままだと人生で○○年分の時間を失う」というインパクトのある数字を提示。行動変容への動機付けを強化する。

### 1.2 データ収集（オンボーディング）

```typescript
// 使用状況ヒアリング
interface UsageAssessment {
  // 1日の平均使用時間（時間単位）
  dailyUsageHours: number;         // 0.5, 1, 2, 3, 4, 5, 6+

  // 1日に開く回数
  dailyOpenCount: number;          // 5, 10, 20, 30, 50+

  // やめたいと思った回数
  quitAttempts: 'never' | 'once' | 'few' | 'many';

  // 主に使用する時間帯
  peakUsageTime: 'morning' | 'afternoon' | 'evening' | 'night' | 'random';

  // いつから使っているか
  usageDuration: '1month' | '6months' | '1year' | '2years' | '3years+';
}
```

### 1.3 計算ロジック

```typescript
interface LifetimeImpact {
  // 年間の損失時間（時間）
  yearlyLostHours: number;

  // 人生で失う推定時間（年単位）
  lifetimeLostYears: number;

  // 具体的な換算例
  equivalents: {
    books: number;           // 読める本の数
    movies: number;          // 見られる映画の数
    skills: string[];        // 習得できるスキル
    travels: number;         // 旅行できる回数
  };
}

// 計算関数
function calculateLifetimeImpact(assessment: UsageAssessment): LifetimeImpact {
  const dailyMinutes = assessment.dailyUsageHours * 60;
  const yearlyHours = (dailyMinutes * 365) / 60;

  // 平均余命を60年と仮定（ユーザー年齢は取得しない）
  const remainingYears = 60;
  const lifetimeHours = yearlyHours * remainingYears;
  const lifetimeLostYears = lifetimeHours / (365 * 24); // 24時間×365日で1年

  return {
    yearlyLostHours: Math.round(yearlyHours),
    lifetimeLostYears: Math.round(lifetimeLostYears * 10) / 10,
    equivalents: {
      books: Math.round(yearlyHours / 5),        // 1冊5時間
      movies: Math.round(yearlyHours / 2),       // 1本2時間
      skills: getLearnableSkills(yearlyHours),
      travels: Math.round(yearlyHours / 50),     // 1旅行50時間
    },
  };
}

function getLearnableSkills(yearlyHours: number): string[] {
  const skills = [];
  if (yearlyHours >= 100) skills.push('新しい言語の基礎');
  if (yearlyHours >= 200) skills.push('楽器演奏');
  if (yearlyHours >= 300) skills.push('プログラミング');
  if (yearlyHours >= 500) skills.push('資格取得');
  return skills;
}
```

### 1.4 表示画面（オンボーディング内）

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  あなたの現状                                                │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│            1日 3時間 × 365日 =                               │
│                                                             │
│                 年間                                         │
│              1,095時間                                       │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  このままだと、人生で                                         │
│                                                             │
│              ┌───────────────────────────┐                  │
│              │                           │                  │
│              │       7.5 年分            │                  │
│              │                           │                  │
│              │    の時間を失います        │                  │
│              │                           │                  │
│              └───────────────────────────┘                  │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  この時間があれば...                                         │
│                                                             │
│    📚 219冊の本を読める                                      │
│    🎬 547本の映画を見られる                                   │
│    🌍 21回の旅行ができる                                      │
│    💻 プログラミングをマスターできる                          │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│    ┌─────────────────────────────────────────────────┐      │
│    │              時間を取り戻す                       │      │
│    └─────────────────────────────────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. カウンセリング・コーチング機能

### 2.1 概要

依存のメカニズム説明、段階的な改善ガイド、失敗時のリカバリーサポートを提供。行動科学に基づいたコーチングコンテンツ。

### 2.2 コンテンツ構成

```typescript
interface CoachingContent {
  // カテゴリ
  category: 'understanding' | 'strategy' | 'recovery' | 'maintenance';

  // コンテンツ
  id: string;
  title: string;
  duration: string;        // '3分で読める'
  content: string;         // Markdown形式
  actionItems: string[];   // 実践できるアクション
}

// コンテンツ一覧
const COACHING_CONTENTS: CoachingContent[] = [
  // 理解する
  {
    category: 'understanding',
    id: 'why-addictive',
    title: 'なぜショート動画はやめられないのか',
    duration: '3分',
    content: `
## ドーパミンの罠

ショート動画が依存性を持つ理由は、脳の報酬系を巧みに刺激するからです。

### 変動報酬（Variable Rewards）
スロットマシンと同じ原理。「次の動画は面白いかも」という期待が...

### 無限スクロール
終わりがないことで、自然な終了ポイントが存在しない...

### パーソナライズ
AIがあなたの好みを学習し、最も刺激的なコンテンツを...
    `,
    actionItems: [
      '今日、自分がどんな時にアプリを開くか意識してみる',
      '開く前に「なぜ開こうとしているか」を3秒考える',
    ],
  },

  // 戦略
  {
    category: 'strategy',
    id: 'implementation-intent',
    title: '「もし〜なら」戦略で習慣を変える',
    duration: '4分',
    content: `...`,
    actionItems: [...],
  },

  // リカバリー
  {
    category: 'recovery',
    id: 'after-relapse',
    title: '失敗しても大丈夫。立て直す方法',
    duration: '3分',
    content: `...`,
    actionItems: [...],
  },

  // 継続
  {
    category: 'maintenance',
    id: 'building-identity',
    title: '「使わない人」になるアイデンティティの構築',
    duration: '5分',
    content: `...`,
    actionItems: [...],
  },
];
```

### 2.3 表示タイミング

```typescript
// コーチングコンテンツの表示ロジック
interface CoachingTrigger {
  trigger: string;
  contentId: string;
  priority: number;
}

const COACHING_TRIGGERS: CoachingTrigger[] = [
  // オンボーディング完了後
  { trigger: 'onboarding_complete', contentId: 'why-addictive', priority: 1 },

  // 初回介入後
  { trigger: 'first_intervention', contentId: 'implementation-intent', priority: 1 },

  // ストリーク途切れ時
  { trigger: 'streak_broken', contentId: 'after-relapse', priority: 1 },

  // 7日継続達成時
  { trigger: 'streak_7_days', contentId: 'building-identity', priority: 2 },

  // 介入を無視して続けた時
  { trigger: 'shield_dismissed', contentId: 'urge-surfing', priority: 1 },
];
```

### 2.4 UI設計

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  < 戻る                     コーチング                       │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  理解する                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🧠  なぜショート動画はやめられないのか                 │   │
│  │      3分で読める                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📱  SNS依存と脳の関係                               │   │
│  │      4分で読める                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  戦略を学ぶ                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🎯  「もし〜なら」戦略で習慣を変える                  │   │
│  │      4分で読める                              ✓ 読了  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  失敗からの回復                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  💪  失敗しても大丈夫。立て直す方法                    │   │
│  │      3分で読める                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 目標設定・日誌機能

### 3.1 概要

ショート動画を見なくなった時間で何をするかを明確にし、その達成状況を簡単にチェックイン。短い振り返り日誌で自己効力感を醸成。

### 3.2 目標設定（オンボーディング）

```typescript
interface AlternativeGoal {
  // メインの代替活動
  primaryActivity: string;       // 自由入力 or テンプレート選択

  // サブ活動（オプション）
  secondaryActivities: string[];

  // 達成基準
  successCriteria: 'did_it' | 'time_spent' | 'completed_task';

  // 目標時間（分/日）
  targetMinutes?: number;
}

// テンプレート
const ACTIVITY_TEMPLATES = [
  { category: '学習', activities: ['読書', '語学学習', 'オンライン講座', '資格勉強'] },
  { category: '健康', activities: ['運動', 'ストレッチ', '瞑想', '散歩'] },
  { category: '創作', activities: ['絵を描く', '音楽', 'ブログ執筆', 'プログラミング'] },
  { category: '人間関係', activities: ['家族と過ごす', '友人に連絡', 'ペットと遊ぶ'] },
  { category: 'その他', activities: ['掃除', '料理', '趣味', '自由入力...'] },
];
```

### 3.3 チェックイン機能

```typescript
interface DailyCheckIn {
  date: string;                   // 'YYYY-MM-DD'
  timestamp: string;              // ISO 8601

  // 代替活動の実行
  didAlternativeActivity: boolean;
  activityNote?: string;          // 何をしたか（50文字以内）
  activityDuration?: number;      // 何分やったか

  // 気分（オプション）
  mood?: 1 | 2 | 3 | 4 | 5;       // 😢😕😐🙂😊

  // 振り返り（オプション、夜に表示）
  reflection?: string;            // 一言振り返り（100文字以内）

  // 自動計測
  interventionCount: number;      // その日の介入回数
  savedMinutes: number;           // 節約した時間
}
```

### 3.4 チェックインフロー

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  今日のチェックイン                                          │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  今日、空いた時間で「読書」できましたか？                     │
│                                                             │
│    ┌─────────────┐      ┌─────────────┐                     │
│    │             │      │             │                     │
│    │     ✓      │      │     ✗      │                     │
│    │   できた    │      │  できなかった │                     │
│    │             │      │             │                     │
│    └─────────────┘      └─────────────┘                     │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  何をしましたか？（オプション）                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 「原因と結果の経済学」を30分読んだ                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  今日の気分は？                                              │
│                                                             │
│     😢    😕    😐    🙂    😊                              │
│                        ↑                                    │
│                      選択中                                  │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│    ┌─────────────────────────────────────────────────┐      │
│    │                   完了                           │      │
│    └─────────────────────────────────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.5 チェックイン表示タイミング

```typescript
// チェックインのトリガー
const CHECK_IN_TRIGGERS = {
  // 1日1回、夜（20:00-22:00）に通知
  eveningReminder: {
    time: '20:00',
    notification: '今日の振り返りをしませんか？',
  },

  // Shield dismissal後（オプション）
  afterIntervention: {
    enabled: true,  // ユーザー設定可能
    message: '代わりに何かできそうですか？',
  },

  // アプリ起動時（前日未完了の場合）
  appLaunch: {
    showIfMissedYesterday: true,
  },
};
```

---

## 4. インタラクティブチュートリアル

### 4.1 概要

TikTok / YouTube Shorts / Instagram Reels のUIを忠実に再現し、アプリの使用方法を実際の体験として説明。「5分経過するとこうなります」を体感させる。

### 4.2 チュートリアルフロー

```
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 導入                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  「実際に体験してみましょう」                                 │
│                                                             │
│  これからTikTokの画面を再現します。                          │
│  いつも通り使ってみてください。                              │
│                                                             │
│  5分経過すると、StopShortsがどのように                       │
│  介入するかを体験できます。                                  │
│                                                             │
│    ┌─────────────────────────────────────────────────┐      │
│    │              体験を始める                        │      │
│    └─────────────────────────────────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Step 2: TikTok UI再現                                       │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │              [動画プレビュー画像]                    │   │
│  │                                                     │   │
│  │                                                     │   │
│  │                                                     │   │
│  │                                                     │   │
│  │   @username                                    ♡   │   │
│  │   動画の説明文がここに表示されます...           💬   │   │
│  │   #ハッシュタグ                                 ↗   │   │
│  │                                                🎵   │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │   ホーム   友達   +   メッセージ   プロフィール       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ↑ スワイプして次の動画へ（シミュレーション）               │
│                                                             │
│  [経過時間: 0:45 / 5:00]                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Step 3: Shield表示（5分経過をシミュレート）                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │                  StopShorts                         │   │
│  │                                                     │   │
│  │             5分が経過しました                        │   │
│  │                                                     │   │
│  │         ────────────────────                        │   │
│  │                                                     │   │
│  │   質の良い睡眠は、明日の自分への投資です              │   │
│  │                                                     │   │
│  │         ────────────────────                        │   │
│  │                                                     │   │
│  │    あなたの約束:                                     │   │
│  │    「深呼吸をして閉じる」                            │   │
│  │                                                     │   │
│  │     ┌─────────────────────────────────────┐         │   │
│  │     │           閉じる                     │         │   │
│  │     └─────────────────────────────────────┘         │   │
│  │                                                     │   │
│  │     もう少し続ける（あと5分）                        │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  これがStopShortsの介入画面です。                            │
│  実際の使用時も、同じように表示されます。                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Step 4: 完了                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│           🎉                                                │
│                                                             │
│    チュートリアル完了！                                      │
│                                                             │
│    これで、StopShortsの使い方を                             │
│    理解できましたね。                                       │
│                                                             │
│    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                             │
│    ポイント:                                                │
│                                                             │
│    ✓ 5分ごとに自動で介入                                    │
│    ✓ あなたの目標をリマインド                               │
│    ✓ 「閉じる」で時間を取り戻す                             │
│                                                             │
│    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                             │
│    ┌─────────────────────────────────────────────────┐      │
│    │              設定を続ける                        │      │
│    └─────────────────────────────────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 UI再現仕様

```typescript
// 再現するアプリUI
interface MockAppUI {
  app: 'tiktok' | 'youtube_shorts' | 'instagram_reels';

  // 表示要素
  mockVideos: MockVideo[];        // プレビュー用のダミー動画
  bottomNav: boolean;             // ボトムナビゲーション
  sideActions: boolean;           // いいね・コメント等のボタン
  header: boolean;                // ヘッダー

  // インタラクション
  swipeEnabled: boolean;          // スワイプでの動画切り替え
  autoProgress: boolean;          // 自動で時間が進む（デモ用）
}

interface MockVideo {
  thumbnailUrl: string;           // ローカルアセット
  username: string;
  description: string;
  hashtags: string[];
  likes: string;                  // '12.3K'
  comments: string;               // '456'
}

// TikTok UI再現コンポーネント
const TikTokMockUI: React.FC<{ onComplete: () => void }> = ({ onComplete }) => {
  // 実装...
};
```

### 4.4 チュートリアルスキップ

```typescript
// スキップオプション（2回目以降のオンボーディング）
const TutorialOptions = {
  // 初回は必須
  firstTime: { skippable: false },

  // 2回目以降（データリセット後など）
  returning: {
    skippable: true,
    skipLabel: 'チュートリアルをスキップ',
  },
};
```

---

## 5. 3日間フリートライアル

### 5.1 概要

課金プラン選択画面で、3日間のフリートライアルをデフォルトの最上位オプションとして表示。ただし、「最初から課金にコミットした方が継続率が高い」という研究データも提示し、意思決定を支援。

### 5.2 プラン構成

```typescript
interface PricingPlan {
  id: string;
  name: string;
  price: number;                // 円
  priceDisplay: string;         // '¥980'
  duration: string;             // '30日間'
  dailyPrice: string;           // '1日約33円'
  features: string[];
  badge?: string;               // 'おすすめ' | '人気'
  isDefault: boolean;           // デフォルト選択
  isTrial: boolean;
}

const PRICING_PLANS: PricingPlan[] = [
  {
    id: 'trial_3days',
    name: '3日間フリートライアル',
    price: 0,
    priceDisplay: '¥0',
    duration: '3日間',
    dailyPrice: '無料',
    features: [
      '全機能が使える',
      'クレジットカード登録不要',
      '自動課金なし',
    ],
    badge: undefined,
    isDefault: true,
    isTrial: true,
  },
  {
    id: 'master_90',
    name: '90日マスター',
    price: 1980,
    priceDisplay: '¥1,980',
    duration: '90日間',
    dailyPrice: '1日22円',
    features: [
      '継続的な遮断',
      'パーソナライズ文言',
      '詳細統計・レポート',
      '週次フィードバック',
    ],
    badge: '最も選ばれている',
    isDefault: false,
    isTrial: false,
  },
  {
    id: 'challenge_30',
    name: '30日チャレンジ',
    price: 980,
    priceDisplay: '¥980',
    duration: '30日間',
    dailyPrice: '1日33円',
    features: [
      '継続的な遮断',
      'パーソナライズ文言',
      '詳細統計',
    ],
    badge: undefined,
    isDefault: false,
    isTrial: false,
  },
  {
    id: 'yearly',
    name: '年間プラン',
    price: 3980,
    priceDisplay: '¥3,980',
    duration: '365日間',
    dailyPrice: '1日11円',
    features: [
      '90日マスター全機能',
      '最もお得',
      '優先サポート',
    ],
    badge: '49%OFF',
    isDefault: false,
    isTrial: false,
  },
];
```

### 5.3 コミットメント効果の表示

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  プランを選択                                                │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  3日間フリートライアル                    ← 選択中  │   │
│  │  ¥0                                                 │   │
│  │                                                     │   │
│  │  ✓ 全機能が使える                                   │   │
│  │  ✓ クレジットカード登録不要                          │   │
│  │  ✓ 自動課金なし                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  90日マスター                       最も選ばれている │   │
│  │  ¥1,980（1日22円）                                   │   │
│  │                                                     │   │
│  │  ✓ 継続的な遮断 + パーソナライズ                     │   │
│  │  ✓ 詳細統計・週次レポート                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  30日チャレンジ                                      │   │
│  │  ¥980（1日33円）                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  年間プラン                                 49%OFF  │   │
│  │  ¥3,980（1日11円）                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│  💡 研究データ                                              │
│                                                             │
│  行動経済学の研究によると、最初から                          │
│  金銭的コミットメントをした人の方が                          │
│  継続率が2.3倍高いというデータがあります。                   │
│                                                             │
│  「もったいない」という心理が、                              │
│  習慣形成を後押しします。                                    │
│                                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│                                                             │
│    ┌─────────────────────────────────────────────────┐      │
│    │              このプランで始める                   │      │
│    └─────────────────────────────────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.4 トライアル終了フロー

```typescript
// トライアル状態管理
interface TrialStatus {
  isTrialActive: boolean;
  trialStartDate: string;         // ISO 8601
  trialEndDate: string;           // ISO 8601
  daysRemaining: number;
  hasShownExpiryWarning: boolean;
}

// トライアル終了時の処理
const handleTrialExpiry = async () => {
  // 1. 機能制限版への自動移行
  await setSubscriptionStatus('free');

  // 2. 通知
  await showExpiryModal({
    title: 'フリートライアルが終了しました',
    body: '3日間の体験、いかがでしたか？',
    stats: await getTrialStats(),  // 3日間の成果
    cta: '有料プランにアップグレード',
  });
};

// トライアル終了1日前の警告
const showTrialExpiryWarning = () => {
  // 「明日でトライアルが終了します」
};
```

---

## 6. 実装優先順位

### Phase 1（MVP v1.0と同時）
1. [ ] インタラクティブチュートリアル（基本版）
2. [ ] 3日間フリートライアル
3. [ ] 目標設定（オンボーディング内）

### Phase 2（v1.1）
4. [ ] インパクト可視化（人生損失時間計算）
5. [ ] 日次チェックイン機能
6. [ ] 簡易日誌機能

### Phase 3（v1.2）
7. [ ] カウンセリングコンテンツ（全記事）
8. [ ] コーチングトリガー実装
9. [ ] チュートリアルの全アプリUI対応（YouTube Shorts, Instagram）

---

## 7. データモデル更新

### 7.1 型定義の追加

```typescript
// src/types/index.ts に追加

// 使用状況アセスメント
export interface UsageAssessment {
  dailyUsageHours: number;
  dailyOpenCount: number;
  quitAttempts: 'never' | 'once' | 'few' | 'many';
  peakUsageTime: 'morning' | 'afternoon' | 'evening' | 'night' | 'random';
  usageDuration: '1month' | '6months' | '1year' | '2years' | '3years+';
}

// 人生への影響計算結果
export interface LifetimeImpact {
  yearlyLostHours: number;
  lifetimeLostYears: number;
  equivalents: {
    books: number;
    movies: number;
    skills: string[];
    travels: number;
  };
}

// 代替活動目標
export interface AlternativeGoal {
  primaryActivity: string;
  secondaryActivities: string[];
  successCriteria: 'did_it' | 'time_spent' | 'completed_task';
  targetMinutes?: number;
}

// 日次チェックイン
export interface DailyCheckIn {
  date: string;
  timestamp: string;
  didAlternativeActivity: boolean;
  activityNote?: string;
  activityDuration?: number;
  mood?: 1 | 2 | 3 | 4 | 5;
  reflection?: string;
  interventionCount: number;
  savedMinutes: number;
}

// トライアル状態
export interface TrialStatus {
  isTrialActive: boolean;
  trialStartDate: string | null;
  trialEndDate: string | null;
  daysRemaining: number;
}

// コーチングコンテンツ
export interface CoachingContent {
  category: 'understanding' | 'strategy' | 'recovery' | 'maintenance';
  id: string;
  title: string;
  duration: string;
  content: string;
  actionItems: string[];
  isRead: boolean;
}
```

### 7.2 Zustand Store 更新

```typescript
// src/stores/useAppStore.ts に追加

interface AppState {
  // ... 既存のフィールド ...

  // 新規追加
  usageAssessment: UsageAssessment | null;
  lifetimeImpact: LifetimeImpact | null;
  alternativeGoal: AlternativeGoal | null;
  checkIns: DailyCheckIn[];
  trialStatus: TrialStatus;
  tutorialCompleted: boolean;
  coachingProgress: Record<string, boolean>;  // contentId -> isRead

  // 新規アクション
  setUsageAssessment: (assessment: UsageAssessment) => void;
  setLifetimeImpact: (impact: LifetimeImpact) => void;
  setAlternativeGoal: (goal: AlternativeGoal) => void;
  addCheckIn: (checkIn: DailyCheckIn) => void;
  startTrial: () => void;
  endTrial: () => void;
  setTutorialCompleted: (completed: boolean) => void;
  markCoachingRead: (contentId: string) => void;
}
```

---

## 8. 画面一覧（追加分）

| 画面 | パス | 説明 |
|-----|------|-----|
| 使用状況ヒアリング | `(onboarding)/usage-assessment.tsx` | 日常の使用時間等を質問 |
| インパクト表示 | `(onboarding)/impact.tsx` | 人生損失時間の表示 |
| チュートリアル | `(onboarding)/tutorial.tsx` | アプリUI再現体験 |
| 目標設定 | `(onboarding)/goal-setting.tsx` | 代替活動の設定 |
| プラン選択 | `(onboarding)/pricing.tsx` | 課金プラン選択 |
| チェックイン | `(main)/check-in.tsx` | 日次チェックイン |
| 日誌一覧 | `(main)/journal.tsx` | 過去のチェックイン履歴 |
| コーチング | `(main)/coaching.tsx` | コーチングコンテンツ一覧 |
| コーチング詳細 | `(main)/coaching/[id].tsx` | 個別コンテンツ表示 |
